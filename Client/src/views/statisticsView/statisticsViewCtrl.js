import './statisticsView.less';

export default class statisticsViewCtrl {
	constructor ($scope, $route, $routeParams, $location, $http) {
		console.log(1);
	}
}

IGDBA.controller('statisticsViewCtrl', statisticsViewCtrl);
